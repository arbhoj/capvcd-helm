apiVersion: v1
kind: Service
metadata:
  name: {{ include "capvcdclusters.fullname" . }}
  labels:
    {{- include "capvcdclusters.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capvcdclusters.selectorLabels" . | nindent 4 }}
